# Test Plan

Planned coverage:
- bitset correctness and masking invariants;
- exact combination enumeration counts;
- key enumeration order and column-mask invariants;
- decoder trajectory invariants on toy fixtures;
- exact-mode enumeration counts;
- direct-bin and finite-state coverage invariants on reduced domains.
